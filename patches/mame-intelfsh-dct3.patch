diff --git a/src/devices/machine/intelfsh.cpp b/src/devices/machine/intelfsh.cpp
index 5b41774ad3c..8d28aebbb94 100644
--- a/src/devices/machine/intelfsh.cpp
+++ b/src/devices/machine/intelfsh.cpp
@@ -111,6 +111,7 @@ DEFINE_DEVICE_TYPE(MACRONIX_29L001MC,        macronix_29l001mc_device,        "m
 DEFINE_DEVICE_TYPE(MACRONIX_29LV160TMC,      macronix_29lv160tmc_device,      "macronix_29lv160tmc",      "Macronix 29LV160TMC Flash")
 DEFINE_DEVICE_TYPE(ST_M29W640GB,             st_m29w640gb_device,             "st_m29w640gb",             "ST M29W640GB Flash")
 DEFINE_DEVICE_TYPE(ST_M29W640FT,             st_m29w640ft_device,             "st_m29w640ft",             "ST M29W640FT Flash")
+DEFINE_DEVICE_TYPE(ST_M28W320ECT,            st_m28w320ect_device,            "st_m28w320ect",            "ST M28W320ECT Flash")
 DEFINE_DEVICE_TYPE(TMS_29F040,               tms_29f040_device,               "tms_29f040",               "Texas Instruments 29F040 Flash")
 
 DEFINE_DEVICE_TYPE(PANASONIC_MN63F805MNP,    panasonic_mn63f805mnp_device,    "panasonic_mn63f805mnp",    "Panasonic MN63F805MNP Flash")
@@ -125,6 +126,7 @@ DEFINE_DEVICE_TYPE(SHARP_LH28F400,           sharp_lh28f400_device,           "s
 DEFINE_DEVICE_TYPE(SHARP_LH28F400,           sharp_lh28f400_device,           "sharp_lh28f400",           "Sharp LH28F400 Flash")
 DEFINE_DEVICE_TYPE(INTEL_E28F008SA,          intel_e28f008sa_device,          "intel_e28f008sa",          "Intel E28F008SA Flash")
 DEFINE_DEVICE_TYPE(INTEL_TE28F160,           intel_te28f160_device,           "intel_te28f160",           "Intel TE28F160 Flash")
+DEFINE_DEVICE_TYPE(INTEL_TE28F800,           intel_te28f800_device,           "intel_te28f800",           "Intel TE28F800 Flash")
 DEFINE_DEVICE_TYPE(SHARP_LH28F160S3,         sharp_lh28f160s3_device,         "sharp_lh28f160s3",         "Sharp LH28F160S3 Flash")
 DEFINE_DEVICE_TYPE(INTEL_TE28F320,           intel_te28f320_device,           "intel_te28f320",           "Intel TE28F320 Flash")
 DEFINE_DEVICE_TYPE(SHARP_LH28F320BF,         sharp_lh28f320bf_device,         "sharp_lh28f320bf",         "Sharp LH28F320BFHE-PBTL Flash")
@@ -163,10 +164,14 @@ intelfsh_device::intelfsh_device(const machine_config &mconfig, device_type type
 		m_device_id2(0),
 		m_device_id3(0),
 		m_maker_id(maker_id),
+		m_device_id_address(2),
 		m_sector_is_4k(false),
 		m_sector_is_16k(false),
 		m_top_boot_sector(false),
 		m_bot_boot_sector(false),
+		m_main_block_size(64 * 1024),
+		m_parameter_block_size(0),
+		m_parameter_block_count(0),
 		m_status(0x80),
 		m_erase_sector(0),
 		m_flash_mode(FM_NORMAL),
@@ -270,6 +275,15 @@ st_m29w640gb_device::st_m29w640gb_device(const machine_config &mconfig, const ch
 st_m29w640ft_device::st_m29w640ft_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock)
 	: intelfsh16_device(mconfig, ST_M29W640FT, tag, owner, clock, 0x800000, MFG_ST, 0x22ed) { m_bot_boot_sector = true; m_device_id2 = 0x2210; m_device_id3 = 0x2200; }
 
+st_m28w320ect_device::st_m28w320ect_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock)
+	: intelfsh16_device(mconfig, ST_M28W320ECT, tag, owner, clock, 0x400000, MFG_ST, 0x88ba)
+{
+	m_device_id_address = 1;
+	m_top_boot_sector = true;
+	m_parameter_block_size = 8 * 1024;
+	m_parameter_block_count = 8;
+}
+
 panasonic_mn63f805mnp_device::panasonic_mn63f805mnp_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock)
 	: intelfsh8_device(mconfig, PANASONIC_MN63F805MNP, tag, owner, clock, 0x10000, MFG_PANASONIC, 0x1b) { m_sector_is_4k = true; }
 
@@ -297,5 +311,13 @@ sharp_lh28f400_device::sharp_lh28f400_device(const machine_config &mconfig, cons
 	: intelfsh16_device(mconfig, SHARP_LH28F400, tag, owner, clock, 0x80000, MFG_SHARP, 0xed) { }
 
+intel_te28f800_device::intel_te28f800_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock)
+	: intelfsh16_device(mconfig, INTEL_TE28F800, tag, owner, clock, 0x100000, MFG_INTEL, 0x9c)
+{
+	m_top_boot_sector = true;
+	m_parameter_block_size = 8 * 1024;
+	m_parameter_block_count = 8;
+}
+
 intel_te28f160_device::intel_te28f160_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock)
 	: intelfsh16_device(mconfig, INTEL_TE28F160, tag, owner, clock, 0x200000, MFG_SHARP, 0xd0) { }
 
@@ -304,7 +327,12 @@ sharp_lh28f160s3_device::sharp_lh28f160s3_device(const machine_config &mconfig,
 	: intelfsh16_device(mconfig, SHARP_LH28F160S3, tag, owner, clock, 0x200000, MFG_SHARP, 0xd0) { }
 
 intel_te28f320_device::intel_te28f320_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock)
-	: intelfsh16_device(mconfig, INTEL_TE28F320, tag, owner, clock, 0x400000, MFG_INTEL, 0x8896) { }
+	: intelfsh16_device(mconfig, INTEL_TE28F320, tag, owner, clock, 0x400000, MFG_INTEL, 0x8896)
+{
+	m_top_boot_sector = true;
+	m_parameter_block_size = 8 * 1024;
+	m_parameter_block_count = 8;
+}
 
 spansion_s29gl064s_device::spansion_s29gl064s_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock)
 	: intelfsh16_device(mconfig, SPANSION_S29GL064S, tag, owner, clock, 0x800000, MFG_SPANSION, 0x227e)
@@ -507,11 +526,14 @@ uint32_t intelfsh_device::read_full(uint32_t address)
 			switch (address & 0xff)
 			{
 				case 0x00: data = m_maker_id; break;
-				case 0x02: data = m_device_id; break;
 				case 0x04: logerror("Block Protection Status not implemented.\n"); break;
 				case 0x06: logerror("Extended Memory Block Verify Code not implemented.\n"); break;
 				case 0x1c: data = m_device_id2; break;
 				case 0x1e: data = m_device_id3; break;
+				default:
+					if ((address & 0xff) == m_device_id_address)
+						data = m_device_id;
+					break;
 			}
 		}
 		else if (m_maker_id == MFG_WINBOND)
@@ -1062,11 +1084,11 @@ void intelfsh_device::write_full(uint32_t address, uint32_t data)
 		switch( m_bits )
 		{
 		case 8:
-			m_data[address] = data;
+			m_data[address] &= data;
 			break;
 		case 16:
-			m_data[address*2] = data >> 8;
-			m_data[address*2+1] = data;
+			m_data[address*2] &= data >> 8;
+			m_data[address*2+1] &= data;
 			break;
 		default:
 			logerror( "FM_WRITEPART1 not supported when m_bits == %d\n", m_bits );
@@ -1186,6 +1208,19 @@ void intelfsh_device::write_full(uint32_t address, uint32_t data)
 
 				m_timer->adjust( attotime::from_msec( duration ) );
 			}
+			else if (m_parameter_block_size && m_parameter_block_count)
+			{
+				uint32_t const byte_address = address * 2;
+				uint32_t const parameter_span = m_parameter_block_size * m_parameter_block_count;
+				bool const parameter_block =
+						(m_top_boot_sector && byte_address >= (m_size - parameter_span)) ||
+						(m_bot_boot_sector && byte_address < parameter_span);
+				uint32_t const size = parameter_block ? m_parameter_block_size : m_main_block_size;
+				uint32_t const base = byte_address & ~(size - 1);
+				memset(&m_data[base], 0xff, size);
+				// Approximate until a per-part erase-duration table is added.
+				m_timer->adjust(attotime::from_seconds(1));
+			}
 			else
 			{
 				// clear the 64k block containing the current address to all 0xffs
diff --git a/src/devices/machine/intelfsh.h b/src/devices/machine/intelfsh.h
index 5274f6b3d3c..d202eb686d6 100644
--- a/src/devices/machine/intelfsh.h
+++ b/src/devices/machine/intelfsh.h
@@ -45,10 +45,14 @@ protected:
 	uint16_t                 m_device_id2;
 	uint16_t                 m_device_id3;
 	uint8_t                  m_maker_id;
+	uint8_t                  m_device_id_address;
 	bool                     m_sector_is_4k;
 	bool                     m_sector_is_16k;
 	bool                     m_top_boot_sector;
 	bool                     m_bot_boot_sector;
+	uint32_t                 m_main_block_size;
+	uint32_t                 m_parameter_block_size;
+	uint8_t                  m_parameter_block_count;
 	uint16_t                 m_page_size;
 
 	// internal state
@@ -236,6 +240,12 @@ public:
 	st_m29w640ft_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock = 0);
 };
 
+class st_m28w320ect_device : public intelfsh16_device
+{
+public:
+	st_m28w320ect_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock = 0);
+};
+
 class panasonic_mn63f805mnp_device : public intelfsh8_device
 {
 public:
@@ -306,6 +316,12 @@ class sharp_lh28f400_device : public intelfsh16_device
 {
 public:
 	sharp_lh28f400_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock = 0);
 };
 
+class intel_te28f800_device : public intelfsh16_device
+{
+public:
+	intel_te28f800_device(const machine_config &mconfig, const char *tag, device_t *owner, uint32_t clock = 0);
+};
+
 class intel_te28f160_device : public intelfsh16_device
@@ -440,6 +457,7 @@ DECLARE_DEVICE_TYPE(MACRONIX_29L001MC,       macronix_29l001mc_device)
 DECLARE_DEVICE_TYPE(MACRONIX_29LV160TMC,     macronix_29lv160tmc_device)
 DECLARE_DEVICE_TYPE(ST_M29W640GB,            st_m29w640gb_device)
 DECLARE_DEVICE_TYPE(ST_M29W640FT,            st_m29w640ft_device)
+DECLARE_DEVICE_TYPE(ST_M28W320ECT,           st_m28w320ect_device)
 DECLARE_DEVICE_TYPE(TMS_29F040,              tms_29f040_device)
 
 DECLARE_DEVICE_TYPE(PANASONIC_MN63F805MNP,   panasonic_mn63f805mnp_device)
@@ -454,5 +472,6 @@ DECLARE_DEVICE_TYPE(SHARP_LH28F400,          sharp_lh28f400_device)
 DECLARE_DEVICE_TYPE(SHARP_LH28F400,          sharp_lh28f400_device)
 DECLARE_DEVICE_TYPE(INTEL_E28F008SA,         intel_e28f008sa_device)
+DECLARE_DEVICE_TYPE(INTEL_TE28F800,           intel_te28f800_device)
 DECLARE_DEVICE_TYPE(INTEL_TE28F160,          intel_te28f160_device)
 DECLARE_DEVICE_TYPE(SHARP_LH28F160S3,        sharp_lh28f160s3_device)
 DECLARE_DEVICE_TYPE(INTEL_TE28F320,          intel_te28f320_device)
